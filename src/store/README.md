# store

Global application state (context providers, reducers, or a state library) for state that
genuinely needs to be shared across pages/features. Prefer local component state and feature-level
state first — promote to `store/` only when needed.
