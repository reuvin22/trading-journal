# styles

The only stylesheet in the project: the Tailwind import, the design tokens (dark + light — see
`DESIGN.md` at the project root), and their mapping into Tailwind's theme. Everything else is
styled with Tailwind utility classes directly in `className` — no other `.css` files should be
added.
