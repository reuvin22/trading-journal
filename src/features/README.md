# features

One folder per domain feature (e.g. `trades/`, `journal/`, `dashboard/`). Each feature folder owns its
components, hooks, and API calls, and exposes a public surface via `index.js`. See `CODING_PREFERENCES.md`
at the project root for the full convention.
